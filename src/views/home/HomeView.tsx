import { PlayerSummary } from '../../components/Home/PlayerSummary';
import { Layout } from '../../components/Layout/Layout';

export const HomeView = () => {
  return (
    <Layout>
      <PlayerSummary />
    </Layout>
  );
};
